export default class Bokning {
  constructor(b_id, tid, datum, namn, tel, assistent, behandling) {
      this.tid=b_id;
      this.tid=tid;
      this.datum=datum;
      this.namn=namn;
      this.tel=tel;
      this.assistent=assistent;
      this.behandling=behandling;
  }
}
