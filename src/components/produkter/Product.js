export default class Product {
    constructor(p_id, p_name, p_size, p_price){
        this.p_id = p_id;
        this.p_name = p_name;//string
        this.p_size = p_size;
        this.p_price = p_price;
    }
}